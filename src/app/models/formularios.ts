export class Formulario {
    id?: string;
    categoriaId: string;
    version: number;
    active: boolean;
    campos: any; // considerar si se debe definir una estructura. La idea es evitar conflictos
    constructor() {

    }
}